<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecoshop.dao.mapper.sys.UserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ecoshop.dao.po.sys.UserRolePO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, role_id, version, created_at, updated_at
    </sql>

    <!-- 根据ID查询用户角色关联 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user_role
        WHERE id = #{id}
    </select>

    <!-- 根据用户ID查询用户角色关联列表 -->
    <select id="selectListByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user_role
        WHERE user_id = #{userId}
    </select>

    <!-- 根据角色ID查询用户角色关联列表 -->
    <select id="selectListByRoleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user_role
        WHERE role_id = #{roleId}
    </select>

    <!-- 新增用户角色关联 -->
    <insert id="insert" parameterType="com.ecoshop.dao.po.sys.UserRolePO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user_role (
            user_id, role_id, version
        ) VALUES (
            #{userId}, #{roleId}, 1
        )
    </insert>

    <!-- 批量新增用户角色关联 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_user_role (
            user_id, role_id, version
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId}, 1)
        </foreach>
    </insert>

    <!-- 根据用户ID删除用户角色关联 -->
    <delete id="deleteByUserId">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <!-- 根据角色ID删除用户角色关联 -->
    <delete id="deleteByRoleId">
        DELETE FROM sys_user_role WHERE role_id = #{roleId}
    </delete>

    <!-- 删除用户角色关联 -->
    <delete id="deleteById">
        DELETE FROM sys_user_role WHERE id = #{id}
    </delete>

</mapper> 