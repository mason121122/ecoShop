<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecoshop.dao.mapper.sys.DictTypeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ecoshop.dao.po.sys.DictTypePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tenant_id, name, type, status, remark, version, created_at, updated_at
    </sql>

    <!-- 根据ID查询字典类型 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dict_type
        WHERE id = #{id}
    </select>

    <!-- 根据字典类型查询 -->
    <select id="selectByType" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dict_type
        WHERE type = #{type}
    </select>

    <!-- 查询字典类型列表 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dict_type
        <where>
            <if test="tenantId != null">
                AND tenant_id = #{tenantId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type LIKE CONCAT('%', #{type}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 新增字典类型 -->
    <insert id="insert" parameterType="com.ecoshop.dao.po.sys.DictTypePO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_dict_type (
            tenant_id, name, type, status, remark, version
        ) VALUES (
            #{tenantId}, #{name}, #{type}, #{status}, #{remark}, 1
        )
    </insert>

    <!-- 修改字典类型 -->
    <update id="update" parameterType="com.ecoshop.dao.po.sys.DictTypePO">
        UPDATE sys_dict_type
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            version = version + 1,
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id} AND version = #{version}
    </update>

    <!-- 删除字典类型 -->
    <delete id="deleteById">
        DELETE FROM sys_dict_type WHERE id = #{id}
    </delete>

    <!-- 批量删除字典类型 -->
    <delete id="deleteBatchIds">
        DELETE FROM sys_dict_type WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper> 