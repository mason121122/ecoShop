<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecoshop.dao.mapper.sys.TenantMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ecoshop.dao.po.sys.TenantPO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="domain" property="domain"/>
        <result column="logo" property="logo"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="contact_email" property="contactEmail"/>
        <result column="expire_time" property="expireTime"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, code, domain, logo, contact_name, contact_phone, contact_email, expire_time,
        status, remark, version, created_at, updated_at
    </sql>

    <!-- 根据ID查询租户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_tenant
        WHERE id = #{id}
    </select>

    <!-- 根据租户编码查询租户 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_tenant
        WHERE code = #{code}
    </select>

    <!-- 查询租户列表 -->
    <select id="selectList" parameterType="com.ecoshop.dao.po.sys.TenantPO" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_tenant
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="code != null and code != ''">
                AND code = #{code}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 新增租户 -->
    <insert id="insert" parameterType="com.ecoshop.dao.po.sys.TenantPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_tenant (
            name, code, domain, logo, contact_name, contact_phone, contact_email,
            expire_time, status, remark, version
        ) VALUES (
            #{name}, #{code}, #{domain}, #{logo}, #{contactName}, #{contactPhone},
            #{contactEmail}, #{expireTime}, #{status}, #{remark}, 1
        )
    </insert>

    <!-- 修改租户 -->
    <update id="update" parameterType="com.ecoshop.dao.po.sys.TenantPO">
        UPDATE sys_tenant
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="domain != null">domain = #{domain},</if>
            <if test="logo != null">logo = #{logo},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            version = version + 1
        </set>
        WHERE id = #{id} AND version = #{version}
    </update>

    <!-- 删除租户 -->
    <delete id="deleteById">
        DELETE FROM sys_tenant WHERE id = #{id}
    </delete>

</mapper> 