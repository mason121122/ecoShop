<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecoshop.dao.mapper.sys.DeptMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ecoshop.dao.po.sys.DeptPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="leader" property="leader"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tenant_id, parent_id, name, code, leader, phone, email, sort,
        status, remark, version, created_at, updated_at
    </sql>

    <!-- 根据ID查询部门 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        WHERE id = #{id}
    </select>

    <!-- 根据部门编码查询部门 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        WHERE code = #{code}
    </select>

    <!-- 查询部门列表 -->
    <select id="selectList" parameterType="com.ecoshop.dao.po.sys.DeptPO" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        <where>
            <if test="tenantId != null">
                AND tenant_id = #{tenantId}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="code != null and code != ''">
                AND code = #{code}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort ASC
    </select>

    <!-- 根据父ID查询部门列表 -->
    <select id="selectListByParentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        WHERE parent_id = #{parentId}
        ORDER BY sort ASC
    </select>

    <!-- 根据角色ID查询部门列表 -->
    <select id="selectListByRoleId" resultMap="BaseResultMap">
        SELECT
        d.<include refid="Base_Column_List"/>
        FROM sys_dept d
        INNER JOIN sys_role_dept rd ON d.id = rd.dept_id
        WHERE rd.role_id = #{roleId}
        ORDER BY d.sort ASC
    </select>

    <!-- 新增部门 -->
    <insert id="insert" parameterType="com.ecoshop.dao.po.sys.DeptPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_dept (
            tenant_id, parent_id, name, code, leader, phone, email,
            sort, status, remark, version
        ) VALUES (
            #{tenantId}, #{parentId}, #{name}, #{code}, #{leader}, #{phone},
            #{email}, #{sort}, #{status}, #{remark}, 1
        )
    </insert>

    <!-- 修改部门 -->
    <update id="update" parameterType="com.ecoshop.dao.po.sys.DeptPO">
        UPDATE sys_dept
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="leader != null">leader = #{leader},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            version = version + 1
        </set>
        WHERE id = #{id} AND version = #{version}
    </update>

    <!-- 删除部门 -->
    <delete id="deleteById">
        DELETE FROM sys_dept WHERE id = #{id}
    </delete>

</mapper> 