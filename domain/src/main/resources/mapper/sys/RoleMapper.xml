<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecoshop.dao.mapper.sys.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ecoshop.dao.po.sys.RolePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="status" property="status"/>
        <result column="data_scope" property="dataScope"/>
        <result column="remark" property="remark"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tenant_id, name, code, status, data_scope, remark, version, created_at, updated_at
    </sql>

    <!-- 根据ID查询角色 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE id = #{id}
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE code = #{code}
    </select>

    <!-- 查询角色列表 -->
    <select id="selectList" parameterType="com.ecoshop.dao.po.sys.RolePO" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_role
        <where>
            <if test="tenantId != null">
                AND tenant_id = #{tenantId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="code != null and code != ''">
                AND code = #{code}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="dataScope != null">
                AND data_scope = #{dataScope}
            </if>
        </where>
    </select>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectListByUserId" resultMap="BaseResultMap">
        SELECT
        r.<include refid="Base_Column_List"/>
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 新增角色 -->
    <insert id="insert" parameterType="com.ecoshop.dao.po.sys.RolePO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (
            tenant_id, name, code, status, data_scope, remark, version
        ) VALUES (
            #{tenantId}, #{name}, #{code}, #{status}, #{dataScope}, #{remark}, 1
        )
    </insert>

    <!-- 修改角色 -->
    <update id="update" parameterType="com.ecoshop.dao.po.sys.RolePO">
        UPDATE sys_role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="status != null">status = #{status},</if>
            <if test="dataScope != null">data_scope = #{dataScope},</if>
            <if test="remark != null">remark = #{remark},</if>
            version = version + 1
        </set>
        WHERE id = #{id} AND version = #{version}
    </update>

    <!-- 删除角色 -->
    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

</mapper> 